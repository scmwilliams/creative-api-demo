<%= form_for(@asset_group) do |f| %>
  <% if @asset_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@asset_group.errors.count, "error") %> prohibited this asset_group from being saved:</h2>

      <ul>
      <% @asset_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :brand_id %><br>
    <%= f.text_field :brand_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
